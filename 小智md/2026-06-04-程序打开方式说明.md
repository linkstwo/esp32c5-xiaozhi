# 2026-06-04 程序打开方式说明

## 结论

当前 `esp小智板` 项目分成两类打开方式：

1. 代码工程
   - 推荐用 `VS Code + ESP-IDF 插件` 打开
   - 主工程目录：`C:\Users\20269\Desktop\esp小智板\text_demo\factory_demo`
2. UI 可视化工程
   - 记录显示曾使用 `SquareLine Studio`
   - 但当前目录下没有找到原始 `.spj` 工程文件，只剩导出后的 LVGL 代码

## 已核对文件

### `text_demo/.vscode/settings.json`

- 设计风格：IDE 工作区配置风格
- 结论：明确存在 `idf.currentSetup`、`openOcdConfigs`、`clangd` 等配置，说明代码工程是按 `VS Code + ESP-IDF` 的方式使用的

### `text_demo/project.info`

- 设计风格：SquareLine 工程元数据风格
- 结论：记录了 `project_name = Smart_Gadget.spj`，说明 UI 最初由 `SquareLine Studio` 导出

## 现状判断

### 代码怎么打开

打开：

`C:\Users\20269\Desktop\esp小智板\text_demo\factory_demo`

适合：

- 看代码
- 改代码
- 编译
- 烧录 ESP32-C5

### UI 怎么打开

当前只能打开导出的 UI 代码，例如：

- `components/ui/ui.c`
- `components/ui/screens/*.c`

如果要用 SquareLine 可视化继续编辑，需要原始 `Smart_Gadget.spj` 文件；目前本地未找到该文件。

