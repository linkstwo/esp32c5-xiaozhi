# 2026-06-04 esp小智板项目全量检查记录

## 本次检查范围

- 检查路径：`C:\Users\20269\Desktop\esp小智板`
- 目标：区分项目本体与官方资料仓库，确认当前已实现内容、未实现内容、关键入口与文件风格

## 总体结论

`esp小智板` 目录下实际包含两部分：

1. `text_demo`
   - 这是当前“智感桌面伴侣/识安”项目的主工作区。
   - 其中真正可编译、可继续开发的核心工程在 `text_demo/factory_demo`。
2. `esp-dev-kits-master`
   - 这是乐鑫官方开发板资料与示例仓库。
   - 主要作用是提供参考实现、板卡文档、上游示例，不是你们当前项目的主业务代码。

文件规模：

- `text_demo/factory_demo`：132 个文件
- `esp-dev-kits-master`：3285 个文件

结论很明确：当前项目本体很小，官方参考仓很大，后续开发应聚焦 `text_demo/factory_demo`。

## 已查看的关键文件与设计风格

### 1. `text_demo/README.md`

- 设计风格：项目总纲/方案说明风格
- 内容作用：定义“智感桌面伴侣”的目标、硬件平台、分层架构、数据流、线程模型和后续模块规划

### 2. `text_demo/progress.md`

- 设计风格：阶段进度看板风格
- 内容作用：记录当前已完成的第一阶段重构、待验证项、后续网络/语音/优化任务

### 3. `text_demo/DEVELOPMENT_PLAN.md`

- 设计风格：技术方案文档风格
- 内容作用：从系统架构、模块职责、接口和开发阶段上给出完整改造方案

### 4. `text_demo/factory_demo/README_CN.md`

- 设计风格：官方示例说明书风格
- 内容作用：描述原始 ESP-SensairShuttle Factory Demo 的功能、硬件需求、操作说明和故障排查

### 5. `text_demo/factory_demo/main/main.cpp`

- 设计风格：精简主入口风格
- 内容作用：初始化板级管理、显示、传感器服务、SquareLine UI，并通过 LVGL 定时器刷新时钟与环境数据

### 6. `text_demo/factory_demo/main/display.cpp`

- 设计风格：板级显示适配风格
- 内容作用：复用 `esp_board_manager` 与 `esp_lvgl_port` 完成 LCD、背光、触摸和 LVGL 端口初始化

### 7. `text_demo/factory_demo/components/sensor_service/sensor_service.hpp/.cpp`

- 设计风格：模块化嵌入式服务风格
- 内容作用：封装 BME690 + BSEC，单独起 FreeRTOS 任务，做 IAQ 运算、NVS 状态持久化、湿度滑动平均

### 8. `text_demo/factory_demo/components/ui/ui.c`

- 设计风格：SquareLine 自动生成 UI 风格
- 内容作用：由可视化工具生成 LVGL 对象、动画和页面结构，偏双屏仪表盘/卡片化展示

### 9. `text_demo/factory_demo/boards/esp_SensairShuttle/board_info.yaml`

- 设计风格：板级元数据配置风格
- 内容作用：标记开发板型号、芯片与版本，属于板卡配置层

### 10. `esp-dev-kits-master/README_CN.md`

- 设计风格：官方资料索引风格
- 内容作用：说明整个乐鑫开发板资料仓库的定位，包含板卡说明、示例代码和文档入口

## 当前工程真实状态

### 已经落地的内容

1. 已从原始 `factory_demo` 中抽离出一个新的最小主程序入口
2. 已保留并接通：
   - LCD 显示
   - 触摸
   - LVGL 运行环境
   - BME690 传感器读取
   - BSEC IAQ 算法
3. 已新增自定义组件：
   - `bme690_sensor`
   - `bsec_iaq`
   - `sensor_service`
   - `ui`
4. UI 已改成双屏展示逻辑，主打时钟 + 环境数据
5. BSEC 状态保存/恢复已经写入代码，目标是通过 NVS 保持校准状态

### 还没有真正落地的内容

1. `README.md` 和 `DEVELOPMENT_PLAN.md` 里写到的网络能力还没开始
   - 没看到 `network_service` 目录
   - 没看到 MQTT/HTTP 实装
2. AI 语音交互还没开始
   - 没看到 `voice_service` 目录
   - 没看到 I2S 麦克风采集、TTS、火山引擎接入代码
3. RGB LED 预警还没落地
4. 文档里提到的 IMU/姿态交互目前没有进入当前主工程的主路径

## 我看到的结构判断

### 项目本体

主路径应认定为：

`C:\Users\20269\Desktop\esp小智板\text_demo\factory_demo`

这是后续改代码、查编译、做功能接入时最该盯住的位置。

### 参考资料库

参考路径应认定为：

`C:\Users\20269\Desktop\esp小智板\esp-dev-kits-master`

这里更像“上游知识库”，适合对照官方板卡实现、找参考组件和看原始示例。

## 额外观察

1. `factory_demo` 下存在 `build` 目录，说明本地做过构建尝试
2. `progress.md` 里明确写着当前 shell 环境下 `idf.py` 不在 PATH，中断点主要在构建验证
3. `components/ui/screens` 仍保留 `Alarm/Call/Chat/Clock/Music_Player/Splash/Weather` 这类 Screen 文件名
   - 这说明 UI 资产是从原始模板或 SquareLine 项目演化来的
   - 文件命名风格偏演示模板风格，但 `main.cpp` 已只拿其中一部分做当前项目展示
4. `小智md` 当前是空的，本记录是第一份项目检查日志

## 后续建议

1. 后续继续看代码时，优先看：
   - `main/main.cpp`
   - `main/display.cpp`
   - `components/sensor_service/`
   - `components/ui/`
2. 如果下一步要推进“识安”比赛版本，应先做：
   - 构建环境打通
   - 实机验证传感器与 UI 刷新
   - RGB LED 异常预警
   - 再决定接语音或云端
3. 如果下一步要做答辩材料，应把“已完成”和“规划中”严格拆开，避免把文档目标说成已实现事实

