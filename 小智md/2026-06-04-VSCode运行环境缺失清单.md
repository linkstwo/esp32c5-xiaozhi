# 2026-06-04 VSCode 运行环境缺失清单

## 结论

当前 `factory_demo` 工程已经能在 VS Code 中打开，但本机还不能编译运行，主要缺失的是 ESP-IDF 工具链。

## 已确认状态

- `VS Code` 已存在
- `Espressif IDF` 扩展已安装
- 工程目录已切到 `factory_demo`
- `git` 已存在
- `python` 已存在

## 缺失项

1. `ESP-IDF`
   - 当前系统没有 `idf.py`
   - 工程也没有可用的 IDF 环境

2. `CMake`
   - 当前系统命令行不可用

3. `Ninja`
   - 当前系统命令行不可用

4. `ESP-IDF` 对应工具链
   - 包括编译器、OpenOCD、相关 Python 环境等

## 额外说明

- `text_demo/.vscode/settings.json`
  - 设计风格：IDE 工作区配置风格
  - 里面写的是旧机器路径：`C:/Users/wzb/...`、`D:/esp32-idf-ahy/...`
  - 这些路径在当前机器上都不存在，属于失效配置

- `project.info`
  - 设计风格：SquareLine 工程元数据风格
  - 说明 UI 曾经由 SquareLine Studio 导出，但这和当前编译问题无关

## 建议下载顺序

优先级最高的是：

1. `ESP-IDF`
2. `CMake`
3. `Ninja`

如果你走官方集成安装方式，通常会把 1、2、3 和工具链一起装掉。

